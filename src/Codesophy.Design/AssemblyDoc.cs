using System.Runtime.CompilerServices;

/// <summary>
/// This assembly contains types and interfaces those are fundamental for the
/// application design and works like an ubiquitous language for it.
/// </summary>
/// 
/// <remarks>
/// <para>
/// Ubiquitous langauge is popular in DDD environments and usually applied to
/// the some specific business domain. In our case software design is a such
/// domain.
/// </para>
/// 
/// <para>
/// The layout of this assembly is trying to be close to the vital building
/// blocks of any application and includes:
/// </para>
/// 
/// <para>
/// The legendary computer scientist Niklaus Wirth was also famous for it's
/// book "Algorithms + Data Structures = Programs". We follow the same pattern
/// and consider the application as a combination of
/// <see cref="Codesophy.Process">processes</see> and
/// <see cref="Codesophy.Model">models</see> respectfully.
/// </para>
/// 
/// <para>
/// Application may be very sophisticated. To deal this high degree of
/// complexity we usually decompose it into smaller modules (including
/// separation of concerns), then this modules into smaller parts and so on
/// till the atomic models and processes. As a result, the application
/// becomes a <see cref="Codesophy.Composition"/> of easily manageable
/// components.s
/// </para>
/// 
/// <para>
/// Application may run in different environments, so we should care about
/// its <see cref="Codesophy.Configuration"/> depends on the environment its
/// executing in.
/// </para>
/// 
/// <para>
/// And the most importan part is to control the
/// <see cref="Codesophy.Quality"/> of the application.
/// </para>
/// 
/// TODO:We can try to use the same namespaces layout in the application we are
/// developing.
/// </remarks>
[CompilerGenerated]
internal class AssemblyDoc
{

}
